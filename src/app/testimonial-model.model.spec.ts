import { TestimonialModel } from './testimonial-model.model';

describe('TestimonialModel', () => {
  it('should create an instance', () => {
    expect(new TestimonialModel()).toBeTruthy();
  });
});
